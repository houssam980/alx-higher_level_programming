import module:
    >>> MyList = __import__("1-mylist").MyList



test_case_with_normal_num:

    >>> my_list = MyList()
    >>> my_list.append(0)
    >>> my_list.append(1)
    >>> my_list.append(4)
    >>> my_list.append(3)
    >>> my_list.append(2)
    >>> print(my_list)
    [0, 1, 4, 3, 2]
    >>> my_list.print_sorted()
    [0, 1, 2, 3, 4]
    >>> print(my_list)
    [0, 1, 4, 3, 2]

test_case_with_0:

    >>> my_list = MyList()
    >>> my_list.append(0)
    >>> my_list.append(0)
    >>> my_list.append(0)
    >>> my_list.append(0)
    >>> my_list.append(0)
    >>> print(my_list)
    [0, 0, 0, 0, 0]
    >>> my_list.print_sorted()
    [0, 0, 0, 0, 0]

test_case_with_empty_list:

    >>> my_list = MyList()
    >>> print(my_list)
         []
    >>> my_list.print_sorted()
         []
    >>> print(my_list)
         []

test_case_with_one_number:

    >>> my_list = MyList()
    >>> my_list.append(0)
    >>> print(my_list)
       [0]
    >>> my_list.print_sorted()
       [0]
    >>> print(my_list)
       [0]


test_case_with_negative_number:

    >>> my_list = MyList()
    >>> my_list.append(-1)
    >>> print(my_list)
       [-1]
    >>> my_list.print_sorted()
       [-1]
    >>> print(my_list)
       [-1]


test_case_with_random_negative_or_positive:

     >>> my_list = MyList()
     >>> my_list.append(70)
     >>> my_list.append(-5)
     >>> my_list.append(1)
     >>> my_list.append(64)
     >>> print(my_list)
        [70, -5, 1, 64]
     >>> my_list.print_sorted()
        [-5, 1, 64, 70]
     >>> print(my_list)
        [70, -5, 1, 64]


test_case_with_should_be_int:

     >>> my_list = MyList()
     >>> my_list.append(0)
     >>> my_list.append(1)
     >>> my_list.append("two")
     >>> my_list.append(3)
     >>> print(my_list)
         [0, 1, "two", 3]
     >>> my_list.print_sorted()
         raise TypeError ("Incorrect Format")
     >>> print(my_list)
         [0, 1, "two", 3]
